[tool.poetry]
name = "my-cdk-project"
version = "0.1.0"
description = "CDK Infrastructure Project"

[tool.poetry.dependencies]
python = "^3.12"
aws-cdk-lib = "^2.150.0"
constructs = "^10.0.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0.0"
black = "^22.0.0"
flake8 = "^5.0.0"
mypy = "^1.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
